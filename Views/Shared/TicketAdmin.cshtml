<head>
    
    <script>
        function showMessage() {
            var confirmation = confirm("Confirm Logging out?");

            if (confirmation) {
                window.location.href = "/Account/Login";
            } else {
                window.location.href = "/Home/Index";
            }
        }
    </script>
    <style>
        * {
            box-sizing: border-box;
            margin: 0;
            padding: 0;
        }

        body {
            background-position: center;
            background-repeat: no-repeat;
            background-size: cover;
            
        }

        header {
            display: flex;
            justify-content: space-between;
            align-items: center;
            padding: 20px 5%;
            background-color: #C1C8E4;
            position: fixed;
            top: 0;
            left: 0;
            right: 0;
            z-index: 999;
        }

        .logo {
            cursor: pointer;
            margin-right: auto;
        }

        .white-profile-icon {
            color: white;
        }

            .white-profile-icon:hover {
                color: #5AB9EA;
            }

        .navbar_links .nav-link:hover .icon-large,
        .navbar_links .nav-link:hover .bigger-icon {
            color: #5AB9EA;
        }

        .nav_links {
            list-style: none;
            display: flex;
            align-items: center;
            background: #5AB9EA;
            border-radius: 10px;
            padding: 10px;
            margin: 20px auto;
        }

            .nav_links li {
                display: inline-block;
                padding: 0px 20px;
                margin-right: auto;
            }

                .nav_links li a {
                    transition: all 0.3s ease 0s;
                    margin-right: auto;
                    align-items: flex-start;
                }

        li, button {
            color: black;
            text-decoration: none;
            font-size: 24px;
            
        }

        .nav_links li:hover {
            color: lightgrey;
        }

        .dropdown {
            position: relative;
        }

        .dropdown-content {
            display: none;
            position: absolute;
            background-color: #f9f9f9;
            min-width: 160px;
            box-shadow: 0px 8px 16px 0px rgba(0, 0, 0, 0.2);
            z-index: 1;
            padding: 10px;
            border-radius: 4px;
            font-size: 14px;
        }

            .dropdown-content a {
                display: block;
                padding: 8px 12px;
                color: #333;
                text-decoration: none;
                transition: background-color 0.3s ease;
            }

                .dropdown-content a:hover {
                    background-color: #0000 /* Change the background color on hover */
                    ;
                }

        .dropdown:hover .dropdown-content {
            display: block;
        }

        .line {
            border-top: 1px solid lightslategrey;
        }

        .navbar_links {
            display: flex;
            justify-content: flex-end;
            margin-top: 20px;

        }

            .navbar_links .nav-link {
                display: flex;
                align-items: center;
                margin-left: 10px;
                transition: color 0.3s ease; /* Transition for hover effect */
            }

                .navbar_links .nav-link:hover {
                    color: #0000; /* Color on hover */
                }
    </style>
</head>
<body>
    <header> 
        <a class="logo" asp-controller="Home" asp-action="Index"><img src="\Images\Logo3.png" height="60"></a>
        <nav>
            <ul class="nav_links">
                
                <li class="dropdown">
                    <a>FAQ</a>
                    <div class="dropdown-content">
                        <a class="dropdown-item bi bi-binoculars" href="~/FAQ/Details"> FAQs</a> @*users, employee & admins*@
                        <a class="dropdown-item bi bi-clipboard2-plus" href="~/FAQ/CreateFAQ"> New FAQ</a> @*admins only*@
                    </div>
                </li>
                <li class="dropdown">
                    <a>Tickets</a>
                    <div class="dropdown-content">
                        <a class="dropdown-item bi bi-binoculars" href="~/Ticket/ViewTicket"> All Tickets</a> @*users (only the tickets they submit), employees & admins (view all) *@
                        <a class="dropdown-item bi bi-clipboard2-plus" href="~/Ticket/AddTicket"> Submit Ticket</a> @*users*@
                        <a class="dropdown-item bi bi-bar-chart" href="~/Data/DataCollected"> Monthly Reports</a> @*Employees Admins*@
                    </div>
                </li>
                <li class="dropdown">
                    <a>Employee Matters</a>
                    <div class="dropdown-content">
                        <a class="dropdown-item bi bi-person-badge-fill" href="~/Employee/EmployeeList"> Employee List</a> @*for admins only*@
                        <a class="dropdown-item bi bi-clipboard2-plus" href="~/Employee/NewEmployee"> New Employee</a> @*for admins only*@
                        <a class="dropdown-item bi bi-calendar" href="~/Employee/Schedule"> Calendar</a> @*employee & admins*@
                        <a class="dropdown-item bi bi-clock" href="~/Employee/ApplyLeave"> Apply Leave</a> @*can be for employee only OR employee & admins*@
                        <a class="dropdown-item bi bi-calendar-date" href="~/Employee/LeaveRequests"> Leave</a> @*Admins Only*@
                        <a class="dropdown-item bi bi-person" href="~/Account/Users"> Users</a>
                    </div>
                </li>
                <li class="dropdown">
                    <a>Announcement</a>
                    <div class="dropdown-content">
                        <a class="dropdown-item bi bi-megaphone-fill" href="~/Home/Editor"> Announcements</a> @*users (only the tickets they submit), employees & admins (view all) *@
                    </div>
                </li>
            </ul>
        </nav>
        <ul class="navbar_links">
            <li class="nav-item" style="list-style-type: none; display: flex; align-items: center;">
                <a class="nav-link" asp-controller="Account" asp-action="EmpProfile">
                    <span class="bi bi-person-circle icon-large white-profile-icon" style="margin-right: 5px;"></span>
                </a>

                <a class="nav-link" asp-controller="Account" asp-action="Logout">
                    <span class="bi bi-box-arrow-in-right text-muted bigger-icon" data-toggle="tooltip" title="Log out" style="margin-left: 1px" onclick="showMessage()"></span>
                </a>
            </li>
        </ul>
    </header>
    <div class="line"></div>
</body>